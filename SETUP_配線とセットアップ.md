# ラインカー セットアップ手順

Mac (Processing スライダー) → UDP → Arduino UNO R4 WiFi → サーボ4個 という構成の組み立てガイドです。

## 全体構成

```
 [Mac / Processing]
   スライダーで速度設定
        │ Wi-Fi (UDP)
        ▼
 [Arduino UNO R4 WiFi]  ← Serial1 ← [ESP32-S3 カメラ]（ライン位置を検出）
   速度＋ステアリング計算
        │ PWM サーボ信号
        ▼
 [FS90R-W サーボ x4]
```

UNO R4 が「頭脳」で、ESP32-S3 は「目（ラインセンサ）」として誤差だけを送る役割です。

## 1. サーボの接続（FS90R-W ×4）

FS90R-W は連続回転サーボなので、**信号線を Arduino の PWM ピンに直結**します。TB6612 は使いません。

| サーボ | 信号線 → UNO R4 ピン |
|--------|----------------------|
| 左前 LF | D3 |
| 左後 LR | D5 |
| 右前 RF | D6 |
| 右後 RR | D9 |

各サーボの線は3本：

- 茶（または黒）= GND
- 赤 = 電源 (＋)
- 橙（または黄/白）= 信号 → 上表のピンへ

### ⚠ 電源は別電源にする

サーボ4個は突入電流が大きく、UNO の 5V ピンからは取れません。**4.8〜6V の電池ボックス（単3×4本やニッケル水素）** などからサーボへ給電し、必ず **電池のGNDと Arduino のGNDを共通に**してください（GND を繋がないと信号が安定しません）。

```
 電池(＋) ──→ 全サーボの 赤
 電池(－) ──┬→ 全サーボの 茶
            └→ UNO R4 の GND   ← 共通GND 必須
```

## 2. ESP32-S3（カメラ）との接続

UNO R4 の **Serial1（D0=RX, D1=TX）** で受け取ります。

| ESP32-S3 | UNO R4 |
|----------|--------|
| TX | D0 (RX) |
| RX | D1 (TX) ※下記の注意 |
| GND | GND |

> ⚠ **ロジック電圧に注意**：ESP32-S3 は 3.3V、UNO R4 は 5V です。
> ESP32 の TX(3.3V) → UNO の RX はそのままで概ね読めますが、
> UNO の TX(5V) → ESP32 の RX(3.3V) は **分圧（抵抗2本）かレベル変換**を入れてください。
> 今回は ESP32→UNO の一方向だけ使えば良いので、UNO の TX は配線しなくても動きます。

ESP32-S3 側は、ライン位置の誤差を `"L:<-100〜100>\n"` の形式で Serial に出力するように作ってください（負=ラインが左、正=右）。

## 3. ソフトの準備

### Arduino 側（`line_car_uno_r4.ino`）
1. Arduino IDE のボードマネージャで **"Arduino UNO R4 Boards"** をインストール。
2. スケッチ冒頭の `WIFI_SSID` / `WIFI_PASS` を自宅の Wi-Fi に書き換え。
3. 書き込み後、**シリアルモニタ（115200bps）** を開くと
   `Connected. IP = 192.168.x.x` と表示される → **この IP をメモ**。

### Processing 側（`line_car_controller.pde`）
1. Processing を起動 → メニュー **スケッチ > ライブラリをインポート > ライブラリを管理**。
2. **"UDP"（作者: hypermedia.net / Stephane Cousot）** を検索してインストール。
3. スケッチ内の `ARDUINO_IP` を、上でメモした UNO R4 の IP に書き換え。
4. 実行（▶）。ウィンドウのスライダーをドラッグ、START / STOP で走行。
   - キーボード：←→ で1%刻み調整、スペースで走行ON/OFF。

> Mac と Arduino は **同じ Wi-Fi ネットワーク**に繋いでください。

## 4. 動作確認のコツ

- まず車体を浮かせて（タイヤを空転）START → スライダーで速度が変わるか確認。
- 前進時に逆回転する側があれば、`.ino` の `DIR_LEFT` / `DIR_RIGHT` の符号を反転。
- 停止時にジワジワ動く場合は、`STOP_US` を 1490〜1510 で微調整、またはサーボ本体のトリマを回す。
- ライン追従が逆に曲がる場合は `Kp` の符号を反転、フラつく場合は `Kp` を小さく。

## 通信プロトコルまとめ

| 方向 | 形式 | 意味 |
|------|------|------|
| Mac → UNO (UDP) | `SPD:75` | 速度 0〜100% |
| Mac → UNO (UDP) | `RUN:1` / `RUN:0` | 走行開始 / 停止 |
| UNO → Mac (UDP) | `ACK speed=.. run=..` | 受信確認 |
| ESP32-S3 → UNO (Serial1) | `L:-100〜100` | ライン誤差 |
